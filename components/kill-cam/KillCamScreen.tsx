import React from 'react';
import ScanScreen from './ScanScreen';

interface Props {}

const KillCamScreen = (props: Props) => {
  const {} = props;
  return (
    <ScanScreen/>
  );
};

export default KillCamScreen;
